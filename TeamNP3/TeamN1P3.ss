;(define (not_main a b)
 ; (display 5)
  ;(newline)
;)

(define (multTest)
	(* 7 2)
	)

(define (plusTest n)
	(+ n 4 6)
	)

(define (plusMultTest n)
	(+ (* n 17) n (* 8 92))
	(+ n 4 6)
	)

(define (ifstmt a b)
	(if(< a b)
		a
		b
		)
	(if(>= a b)
		b
		)
	)

(define(funcAll a b c)
	(plusTest a b)
	)